<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.velasquez.asistentevirtualucv">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.CrearCuenta2Activity"></activity>
        <activity android:name=".Views.CrearCuenta1Activity" />
        <activity android:name=".Views.CrearCuentaActivity" />
        <activity android:name=".Views.MainActivity" />
        <activity android:name=".Views.AgregarCursoActivity" />
        <activity android:name=".Views.AgrearDocenteActivity" />
        <activity android:name=".Views.IniciarSesionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>